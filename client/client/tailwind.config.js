/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}",
  ],
  theme: {
    extend: {
      // display:['group-focus'],
      // opacity:['group-focus'],
      // inset:['group-focus'],
    },
  },
  plugins: [],
}
